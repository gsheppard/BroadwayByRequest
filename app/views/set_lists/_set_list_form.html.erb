<%= form_for [@performance, @set_list] do |f| %>
  <%= f.simple_fields_for :song_list do |field| %>
    <% Song.all.each do |song| %>
      <%= field.input "#{song.id}", label: song.name, as: :boolean %>
    <% end %>
  <% end %>
  <%= f.submit %>
<% end %>
