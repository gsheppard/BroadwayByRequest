<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <title><%= content_for?(:title) ? yield(:title) : "Broadway By Request" %></title>

    <%= stylesheet_link_tag    "application" %>
    <%= javascript_include_tag "vendor/modernizr" %>
    <%= csrf_meta_tags %>
  </head>

  <body>

    <div class="contain-to-grid">
      <nav class="top-bar" data-topbar>
        <ul class="title-area">
          <li class="name">
            <h1><%= link_to "Broadway By Request", root_path %></h1>
          </li>
        </ul>

        <section class="top-bar-section">

          <!-- Left Nav Section -->
          <ul class="left">
            <li><%= link_to "Actors", actors_path %></li>
            <li><%= link_to "Musicals", musicals_path %></li>
          </ul>
        </section>
      </nav>
    </div>

    <div class="row">
      <%= yield %>
    </div>

    <%= javascript_include_tag "application" %>
  </body>
</html>
